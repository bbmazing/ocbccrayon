OCBC CRAYON 2026 — Generative AI & LLMs: Theory
Speaker notes · 39 slides
================================================

Slide 1 — Generative AI & LLMs · Theory
  • Welcome to Session 3. This assumes Topics 1 and 2 — you can wrangle data and make it predict. Today: making machines generate language.
  • 90 minutes, intuition-first: no matrix math. One animated demo, banking flavour, and we close by spotting today's theory inside OCBC's own AI products.
  • Promise: by the end you can read a GenAI system end to end and talk to the people building them.

Slide 2 — The idea that started it
  • Open on the single most important idea in modern AI — the 2017 paper title. It doubles as a teaser for Part 2.
  • Everything today — GPT, Claude, Gemini, Llama — descends from this one architectural idea: attention.
  • We will unpack exactly what attention means, with pictures, in the transformer section.

Slide 3 — From predicting data to generating language
  • Bridge from Topic 2. There we made data predict a label or a number. GenAI goes one step further: it generates new content.
  • Predictive ML answers a yes/no question like “is this fraud?” Generative AI writes the suspicious-activity summary. Same data foundation, a brand-new capability.
  • Frame today as adding generation on top of the prediction skills they already have.

Slide 4 — Discriminative sorts · generative creates
  • The core distinction, in plain terms. A discriminative model sorts an input into a category — it draws a line between classes and tells you which side you are on. That was all of Topic 2.
  • A generative model instead produces brand-new content in the style of its training data — here, text. It does not choose from a fixed list of answers; it writes one.
  • Banking pair to make it stick: a discriminative model flags a transaction as suspicious; a generative model then drafts the suspicious-activity report (SAR) narrative for an analyst to review.

Slide 5 — LLMs are the language engine of GenAI
  • Place LLMs in the landscape. Generative AI is the umbrella — it spans text, images, audio and code.
  • LLMs are the text/language engine of that umbrella. Today is entirely about the text branch.
  • Many bank use cases are multimodal at the edges, but the language model is the core we are studying.

Slide 6 — The theory you will see in our own tools
  • One grounding slide before the theory. LLMs are already inside the bank — these three are the running showcase.
  • Document extraction reads KYC forms; an internal assistant helps relationship managers; a customer assistant answers in-app.
  • Promise: every concept today shows up in one of these. We revisit them at the end.

Slide 7 — Part 1 — Large Language Models
  • Part 1, about 25 minutes. We define a language model, show next-token prediction, and walk the history.
  • Keep it concrete: tokens, embeddings, scale, the three-stage training recipe, and where LLMs fail.
  • Ends with a live demo of a single API call, then grounding with RAG.

Slide 8 — Autocomplete on steroids
  • Plainest possible definition: a language model assigns probabilities to sequences of words, and its whole job is to predict the next token — roughly, the next word.
  • The one-liner that sticks: autocomplete on steroids. Your phone keyboard does a tiny version of this every time it suggests your next word.
  • Everything that looks like intelligence is this one move — predict, append, repeat — done extremely well, over and over.

Slide 9 — It predicts a distribution, then picks
  • Make it concrete. Given the prompt, the model produces a probability for every possible next token — that whole set of probabilities is the “distribution”. (We show whole words here for readability; strictly, each candidate is a token, a word-piece — unpacked on the next slide.)
  • Here the top candidate is “balance”, then “frozen”, then “overdrawn”. It then samples from the distribution — usually a likely word, not always the very top one, which is what keeps the output varied rather than robotic.
  • Repeat token by token, feeding each pick back in, and you get fluent text. The whole craft is shaping this distribution well.

Slide 10 — Words become numbers the model can do math on
  • Two ideas here. First, text is broken into tokens — short word-pieces — because the model works in fixed units, not free-form letters. A common word like “banking” may be one token; a rare word is split into several.
  • Second, each token is turned into a vector: a long list of numbers called an embedding. The picture here is the classic word-embedding intuition — similar meanings get similar numbers and land close together. Inside the transformer this starting vector is then adjusted by context, which is exactly what attention (coming up) does.
  • Why numbers and not words? A model can only do arithmetic, so meaning has to become geometry. “Debit” sits near “credit”; “king” is far away. Topic 4 reuses this same idea at a larger grain — embedding whole sentences or passages so we can find the ones closest in meaning to a question.

Slide 11 — Scale unlocked learning from the prompt
  • “Large” is three things multiplied: parameters (the model's learned weights), training data, and compute. GPT-3 had 175 billion parameters.
  • Scale did something surprising — it unlocked in-context learning: show the model two labelled examples in the prompt and it copies the pattern on the third, with no retraining at all.
  • Some abilities seem to appear only past a certain size — so-called “emergent” abilities, though researchers debate how sharp that jump really is. We lean on in-context learning directly in the prompting section.

Slide 12 — Sixty years to an overnight success
  • Sixty years in one minute. ELIZA faked conversation with rules; statistical NLP counted word frequencies.
  • word2vec (2013) gave words vectors; the Transformer (2017) changed everything; GPT and BERT (2018) started the pretraining era.
  • GPT-3 (2020) showed in-context learning at 175B; ChatGPT (2022) brought it to everyone. Today: GPT, Claude, Gemini, Llama — multimodal and reasoning.

Slide 13 — Three stages: pretrain, fine-tune, align
  • How a modern LLM is actually made, in three stages. Stage 1, pretraining: predict the next token across a huge slice of the internet. This is “self-supervised” — the text is its own answer key, so no human labelling is needed. It learns language and world knowledge, but not yet how to behave.
  • Stage 2, instruction tuning: fine-tune on examples of instructions paired with good answers, so the model learns to answer a request rather than just continue the text.
  • Stage 3, RLHF (reinforcement learning from human feedback): people rank competing answers and the model is nudged toward the preferred ones — helpful, honest, harmless. Stages 2 and 3 are what turn a raw next-word predictor into an assistant.

Slide 14 — A brilliant drafter, an unreliable oracle
  • Honest framing for a risk-aware audience. Strengths: drafting, summarising, extraction, translation, code.
  • Limits: hallucination — confidently wrong; no live knowledge without retrieval; a fixed context window — it can only read so much text at once, and your prompt, any pasted or retrieved text, and the answer all share that one budget; weak at exact arithmetic; and it can inherit bias from its training data.
  • The point: treat output as a capable draft to verify, never as ground truth. This sets up RAG and guardrails.

Slide 15 — RAG: retrieve, augment, generate
  • The fix for hallucination and stale knowledge: retrieval-augmented generation. Do not rely on the model's memory.
  • Retrieve relevant passages from a trusted knowledge base, augment the prompt with them, then generate the answer from that context.
  • Banking framing: answer from our policy documents, with citations — not from whatever the model happened to memorise.

Slide 16 — The model is one API call
  • Demo 1, replayed in-deck — press R or Replay. The model itself is a single API call.
  • Cell 2 is a bare call: prompt in, completion out. Cell 3 adds a retrieved policy snippet inside a tagged <context> block, with a system instruction to answer only from it — the RAG pattern — and the answer becomes grounded and specific.
  • The lesson: the model is one API call; the real engineering is the context and prompt you wrap around it — keep retrieved text clearly separated from the user's question, never blindly concatenated.

Slide 17 — Part 2 — The Transformer
  • Part 2, about 25 minutes. Pure intuition — attention links, parallel heads, stacked blocks. No matrix math.
  • Start with why older models hit a wall, then the 2017 breakthrough, then self-attention shown visually.
  • End by connecting the architecture back to reading and drafting bank text.

Slide 18 — Reading word-by-word hits a wall
  • Set up the problem the transformer solved. Older models — RNNs, or recurrent neural networks — read a sentence one word at a time, carrying a running summary forward, like reading with a single finger.
  • Two costs: it is slow, because each step waits for the one before it; and it forgets long-range context — by the end of a long paragraph the beginning has faded.
  • The transformer's move: read the whole sequence at once, in parallel, and let every word look directly at every other word, near or far.

Slide 19 — Attention Is All You Need
  • The landmark. Vaswani and colleagues at Google, 2017. The radical claim in the title: you can drop recurrence entirely.
  • Replace it with attention, and process the sequence in parallel. Simpler, faster, and it scaled far better.
  • This single architecture underlies GPT, BERT, Claude, Gemini and Llama. The rest of Part 2 is what is inside it.

Slide 20 — Every word weighs every other word
  • This is the heart of the transformer. To understand a word, the model lets it look at every other word and weigh how relevant each one is — that weighing is what “attention” means.
  • Why it matters: “charge” could be a fee or an accusation. By attending strongly to “disputed”, the model works out which sense is meant here — it resolves meaning from context.
  • No matrix math today — just read the picture: a thicker line means more attention. The focus word “charge” links most to “disputed”, and also to “customer” and “card”.
  • One honest caveat for later: a BERT-style encoder really does let every word see every other word. A decoder LLM — the kind that generates text — lets each word see only the words before it, and that left-to-right limit is exactly what lets it predict the next one.

Slide 21 — Many readers, each catching different patterns
  • One attention pattern catches one kind of relationship; a real transformer runs many in parallel — these are the “heads”.
  • Different heads end up sensitive to different things — some to grammar, some to meaning, some to reference — but those roles are learned, not assigned, and most heads do not map cleanly to a single human label. The honest takeaway: many heads in parallel capture more kinds of relationship than one could alone.
  • Analogy: a panel of readers, each catching different threads and pooling their notes for a richer read — just do not picture neat, labelled job titles.

Slide 22 — What else is in a transformer block
  • Round out the block in plain terms. Positional encoding tags each token with its position, because attention on its own is order-blind — without it, “bank charged a fee” and “fee charged a bank” would look identical.
  • Feed-forward: each token gets a little more processing on its own. Residual connections are shortcuts that let a layer's input skip ahead and be added back, so very deep stacks do not lose the original signal; layer norm just keeps the numbers in a stable range. Together they make deep models trainable.
  • Attention plus these supporting pieces is one transformer block — the unit you stack over and over.

Slide 23 — Stack the block, deepen the model
  • Now repeat. Stack many identical blocks and you get a deep network — each layer refines the representation.
  • More layers, more parameters, more data — more capability. GPT-3 stacked 96 of these blocks.
  • This is the concrete meaning of large from Part 1 — depth times width times data.

Slide 24 — Encoder understands · decoder generates
  • One clean contrast. Encoder models like BERT read the whole text to understand it — great for classification and search.
  • Decoder models like GPT, Claude and Gemini generate text left to right — great for writing and chat.
  • Most chat LLMs today are decoder-only. That is the family behind the customer assistant.

Slide 25 — The same machinery reads our bank text
  • Zoom back out before prompting. The very same attention machinery is what reads bank text and drafts summaries.
  • Read the note, attend to what matters, generate the summary token by token — that is the whole pipeline.
  • This is the bridge to Part 3: now that we have a capable model, how do we steer it?

Slide 26 — Part 3 — Prompt Engineering
  • Part 3, about 23 minutes — the most immediately useful section for everyone in the room.
  • We climb the lab-recommended ladder of techniques, with a banking example for each.
  • End on responsible use in a bank: hallucination, injection, privacy, human-in-the-loop.

Slide 27 — Steer the model with words, not retraining
  • Define it. Prompt engineering is steering a fixed model with the words you give it — no weight changes.
  • It is far faster and cheaper than fine-tuning, preserves the model's general knowledge, and keeps working across model updates.
  • Reach for prompting first; fine-tuning is a later, heavier tool — and note that fine-tuning changes the model's behaviour and style, not the facts it knows. For facts that change, the answer is retrieval (Topic 4).

Slide 28 — Climb only as high as you need
  • The spine of the section — the lab-recommended ladder, in impact order. Start simple, add only what you need.
  • Be clear and direct, then give examples, then ask for step-by-step reasoning, then add structure, a role, and finally chain prompts.
  • Most tasks are solved by the first two or three rungs. Climb only as far as the problem demands.

Slide 29 — Specific beats clever
  • Rung 1, the highest-leverage. Vague prompts get vague answers. Say exactly what you want, with context and motivation.
  • The colleague test: if a new colleague would be confused by your instruction, so is the model.
  • Here, the specific version names the role, the format, and the goal — and gets a usable answer first time.

Slide 30 — Show, don't just tell
  • Rung 2, few-shot prompting — “few-shot” just means giving the model a few worked examples before the real input. Do not only tell it; show it.
  • Three to five diverse examples teach format and tone better than any description. Banking example: a couple of labelled transaction-to-category pairs, then the new transaction to classify.
  • The model copies the pattern. Keep the examples varied so it generalises rather than memorising one shape.

Slide 31 — Ask it to think step by step
  • Rung 3, chain-of-thought. For multi-step problems, ask the model to reason step by step before giving its answer — accuracy jumps, because it works through the problem instead of blurting a guess.
  • Also give it explicit permission to say “I don't know”, which cuts down confident guessing.
  • Trade-off: it is slower and uses more tokens, so save it for tasks that genuinely need working-out, not simple lookups.

Slide 32 — Separate the parts, set the persona
  • Rungs 4 and 5 together. Structure your prompt with clear sections or tags so the model never mixes instructions with data.
  • Assign a role — you are a compliance analyst — to set tone, vocabulary and expertise.
  • Tags like context, instructions and data are especially useful when you paste in documents.

Slide 33 — Break big tasks into a chain
  • Rung 6. For big tasks, do not ask for everything at once. Break it into a chain of focused steps.
  • Draft, then review, then improve — each step gets the model's full attention, and the self-review catches errors.
  • Chaining trades a little latency for markedly higher quality on complex work.

Slide 34 — Use it responsibly in a bank
  • Close Part 3 with responsible use — guardrails, not a compliance lecture. Hallucination: verify facts before acting.
  • Prompt injection: untrusted text can carry hidden instructions, so never trust pasted content blindly.
  • Privacy: no sensitive customer data into unapproved tools. And keep a human in the loop for any real decision.

Slide 35 — Showcase — GenAI at OCBC
  • Bring it home. Everything today shows up in OCBC's own AI products.
  • Three illustrative examples, mapped to the exact concepts we covered.
  • Where internal details are unknown, we describe generically and label as illustrative.

Slide 36 — Spot today's concepts at OCBC
  • Map each product back to a concept. Document extraction is tokens and embeddings turning forms into structured fields.
  • The RM-support chatbot is RAG plus prompting — grounded answers from policy and product knowledge.
  • The customer assistant is a decoder LLM with grounding and guardrails. Illustrative — real products, simplified.

Slide 37 — OCBC runs its own open model, in-house
  • OCBC now runs its own open-weight model in-house: Qwen3.5-35B-A3B, served on NVIDIA H200 GPUs. Open-weights means the parameters are published, so we can download and host the model ourselves instead of calling an external API.
  • The A3B in the name means mixture-of-experts: of about 35 billion parameters, only around 3 billion are active for any given token. You get large-model quality at small-model inference cost and speed, which is why it serves efficiently on H200s, each carrying 141 GB of memory.
  • Why on-premise: customer and bank data never leaves our environment, which is essential for privacy, data residency and regulation. We also gain full control to fine-tune and guardrail, predictable cost at scale, and independence from any single provider.
  • Tie back to the theory: this is still a decoder-only transformer doing next-token prediction. The difference is open weights running on our own hardware.

Slide 38 — Three things to remember
  • Replay the three spines. What an LLM is: next-token prediction, trained in three stages.
  • The transformer: attention, processed in parallel, stacked deep. Prompting: the ladder from clear-and-direct up to chaining.
  • Three takeaways: it is prediction not magic; steer with prompts before fine-tuning; verify and use responsibly.

Slide 39 — Wrap — You can now read a GenAI system end to end.
  • Close it. They can now read a GenAI system end to end — and recognise it in OCBC's own tools.
  • Pointers to go deeper: Anthropic's prompt-engineering docs for practice, and the original paper for the brave.
  • Thank them and open the floor for questions.
